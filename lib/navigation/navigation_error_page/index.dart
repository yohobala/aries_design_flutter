import 'package:flutter/material.dart';

class AriNavigationErrorPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Error: Page not found'),
      ),
    );
  }
}
